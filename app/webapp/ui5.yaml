specVersion: "3.0"
metadata:
  name: itsm.ui
type: application
resources:
  configuration:
    paths:
      webapp: .
builder:
  resources:
    excludes:
      - "/node_modules/**"
      - "/dist/**"
      - "/package.json"
      - "/package-lock.json"
      - "/ui5.yaml"
